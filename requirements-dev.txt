# Development and testing dependencies.
# Install: uv pip install -r requirements-dev.txt
# (This file already includes all runtime deps via -r requirements.txt)

-r requirements.txt

# Testing
pytest>=7.4
pytest-snapshot>=0.9
httpx>=0.27             # FastAPI TestClient
